# 內含 API key，永遠不要提交
config.json

# 儲存設定時的暫存檔，正常會被 rename 掉；程式中途被砍才會留下
config.json.*.tmp

# 流量記錄的落檔，內含專案目錄與 session id
traffic.log
traffic.log.1

# Claude Code 本地設定：內含機器專屬的 router 位址，不進版控
.claude/

# impeccable 設計工具的本機狀態（decision.json 記錄的視覺世界、外部圖片參照等）
.impeccable/

# 零 npm 依賴是刻意的約束，但還是要擋掉萬一手滑裝進來的東西
node_modules/

.env
.DS_Store
