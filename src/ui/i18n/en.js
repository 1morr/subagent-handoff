/*
 * 英文語系目錄。預設語言 —— navigator.language 判斷不出 zh 開頭時，或
 * localStorage 沒有存過使用者的選擇時，就是這一份。
 *
 * 純資料檔：一個扁平的 key → 字串 map，沒有巢狀、沒有邏輯。key 用點號分隔命名空間
 * 方便閱讀（例如 `rack.emptyState`），純粹是慣例，app.js 的 t() 不特別解析它。
 * 插值語法是 `{name}`，t(key, { name: value }) 會原樣替換，不做 HTML escape ——
 * 呼叫端已經在組 HTML 字串時另外過 esc()，這裡重複跳脫反而會把 &amp; 變成 &amp;amp;。
 */
export default {
  // ── 共用 ──────────────────────────────────────────────────────
  'common.save': 'Save',
  'common.saved': 'Config saved',
  'common.unsaved': 'Unsaved changes',
  'common.savedToast': 'Saved',
  'common.saveFailed': 'Save failed: {message}',
  'common.actionFailed': 'Action failed: {message}',
  'common.loading': 'Loading…',
  'common.copy': 'Copy',
  'common.copied': 'Copied',
  'common.preview': 'Preview',
  'common.test': 'Test',
  'common.testing': 'Testing…',
  'common.name': 'Name',
  'common.filterAll': 'All',
  'common.filterAttn': 'Attention only',
  'common.mainConversation': 'Main conversation',
  'common.allSubagents': 'All subagents',
  'common.subLine': 'Subscription line',
  'common.prvLine': 'Third-party line',
  'common.blockedByUpstream': 'Blocked by upstream',
  'common.claudeBackedOff': "Claude Code backed off on its own",
  'common.routerRelayed': 'router just relayed it as-is',
  'common.inheritGlobal': 'inherit global',
  'common.retry': 'Retry',
  'common.noRetry': "Don't retry",
  'common.blankMeansForwardAsIs': 'blank = forward as-is',
  'common.restartRequiredBadge': 'restart required',
  'common.moveUp': 'Move up',
  'common.moveDown': 'Move down',
  'common.langLabel': 'Language',

  // ── 導覽列 ────────────────────────────────────────────────────
  'nav.bay': 'Rack',
  'nav.providers': 'Providers',
  'nav.rules': 'Routing',
  'nav.logs': 'Traffic',
  'nav.advanced': 'Advanced',
  'nav.setup': 'Connect',

  // ── 重啟提示 ──────────────────────────────────────────────────
  'restart.required': '{reasons} changed — restart <code>npm start</code> for it to take effect.',
  'restart.note': 'Everything else is read fresh on every request and takes effect as soon as you save.',
  'restart.reasonFallback': 'Settings',
  'restart.reason.port': 'Port',
  'restart.reason.trafficLog': 'Traffic log file',
  'restart.listSep': ', ',

  // ── 進條共用 ──────────────────────────────────────────────────
  'rack.col.seat': 'Seat',
  'rack.col.time': 'Time',
  'rack.col.source': 'Source',
  'rack.col.dir': 'Directory',
  'rack.col.requestedModel': 'Requested model',
  'rack.col.sentModel': 'Sent model',
  'rack.col.status': 'Status',
  'rack.col.duration': 'Duration',
  'rack.col.thinking': 'Thinking',
  'rack.col.note': 'Note',
  'rack.effortRemoved': '{effort} → removed',
  'rack.emptyFiltered': 'No strips under this filter.',
  'rack.emptyAll': 'The rack is empty — router is listening, but no requests have come in yet.',
  'rack.resetsIn': 'resets in {secs}s',
  'rack.resetsAt': 'resets at {time}',
  'rack.target.passthrough': 'passthrough (subscription)',
  'rack.target.notSent': 'not sent',
  'rack.note.fetchFailed': "router couldn't reach upstream · {error}",
  'rack.note.aborted': "not an error · Claude Code backed off on its own",
  'rack.note.blockedRetryAfter': 'upstream blocked it · retry in {secs}s',
  'rack.note.blockedReset': 'upstream blocked it · {reset}',
  'rack.note.blocked': 'upstream blocked it · router just relayed it as-is',
  'rack.note.streamError': '200 but the stream carried an error event',
  'rack.note.effortStripped': '/effort got eaten by dropFields',
  'rack.note.retried': 'router retried {attempts}x before it succeeded',
  'rack.note.pings': 'sent {pings} keep-alive ping(s)',
  'rack.note.cwdUnknown': "cwd table not built yet",
  'rack.note.noSession': "no session id · not sent by Claude Code",
  'rack.ann.upstreamSaid': 'Upstream said',
  'rack.ann.aborted': 'Aborted',
  'rack.ann.fetchFailed': "router couldn't reach upstream",
  'rack.ann.ruleHit': 'Rule matched',
  'rack.ann.noRuleHit': 'no rule matched · {target}',
  'rack.ann.retried': 'Retried',
  'rack.ann.retriedValue': 'sent {attempts}x · {chain}',
  'rack.ann.noReasonRecorded': 'reason not recorded',
  'rack.ann.rateLimited': 'Rate limited',
  'rack.ann.countdown': 'Countdown',
  'rack.ann.countdownValue': "Upstream didn't send retry-after — the countdown Claude Code shows is something it computed itself",
  'rack.ann.silentDowngrade': 'Silent downgrade',
  'rack.ann.silentDowngradeValue': '/effort was stripped before sending ({effort} → none). The request still returns 200, the model just got dumber',
  'rack.ann.fix': 'Fix',
  'rack.ann.fixValue': 'Go to Providers and remove output_config from the dropped body fields',
  'rack.ann.keepAliveValue': 'router inserted {pings} ping(s) · upstream was quiet for over 60s',
  'rack.ann.rewritten': 'Rewritten before sending',
  'rack.ann.shape': 'Request shape',
  'rack.ann.shapeMessages': '{n} messages',
  'rack.ann.hasSystem': 'has system',
  'rack.ann.noSystem': 'no system',
  'rack.ann.streaming': 'streaming',
  'rack.ann.nonStreaming': 'non-streaming',
  'rack.ann.dir': 'Directory',
  'rack.ann.dirUnknown': "unknown — this session's main conversation hasn't sent a request yet, so the cwd table isn't built",
  'rack.ann.noSessionFull': "none — this wasn't sent by Claude Code, something else hit the router's port",
  'rack.ann.time': 'Time',

  // ── 機架分頁 ──────────────────────────────────────────────────
  'bay.noProviderSet': 'not set',
  'bay.quotaWindowUsed': '5-hour quota window · used',
  'bay.rateLimitReported': 'rate limit info reported',
  'bay.quotaWindow': '5-hour quota window',
  'bay.noRateLimitYet': "upstream hasn't reported a rate limit yet",
  'bay.segSub': 'SUB subscription',
  'bay.qtyPct': '{count} requests · {pct}%',
  'bay.noTrafficBand': 'No traffic in the last 5 minutes. Once Claude Code points here, this band will show the routing split.',
  'bay.recentSplit': 'Split over the last 5 minutes',
  'bay.routedCount': '{count} requests',
  'bay.blockedCount': '{count} blocked',
  'bay.liveHint': 'Settings take effect immediately · the next request reroutes',
  'bay.subSeat': 'Subscription seat',
  'bay.mainPlusAllSubagents': 'Main conversation + all subagents',
  'bay.inFlight': 'In-flight requests',
  'bay.last4min': 'last 4 min: {count}',
  'bay.loadTitle': 'last 4 minutes, 30s per bar',
  'bay.credentialsPassthrough': 'credentials forwarded as-is',
  'bay.noRetryOnThrottle': "throttling isn't retried",
  'bay.thirdPartySeat': 'Third-party seat · {name}',
  'bay.noTrafficNow': 'No traffic right now',
  'bay.sentModelLabel': 'Model sent',
  'bay.noRewrite': 'not rewritten, forwarded as-is',
  'bay.noBaseUrl': 'base URL not set',
  'bay.flip': 'Hand back to subscription · effective immediately',
  'bay.unflip': 'Take subagents back',
  'bay.flipToast': 'All subagents handed off to the subscription · next request reroutes',
  'bay.unflipToast': 'Subagents taken back',
  'bay.strips': 'Strips',
  'bay.stripsHint': 'Click a strip to expand its notes · refreshes every 3s',

  // ── 圖例 ──────────────────────────────────────────────────────
  'legend.title': 'Rack flags',
  'legend.clr': 'Clear',
  'legend.chk': 'Succeeded but something happened · nudged out half a slot',
  'legend.hold': 'Blocked · nudged out a full slot + alarm outline',
  'legend.hint': 'Tab color is the <strong>seat</strong> (teal = subscription, amber = third-party); the flag and how far it is pushed out are the status',

  // ── Providers 分頁 ────────────────────────────────────────────
  'providers.intro': 'A provider must expose an <strong>Anthropic Messages–format</strong> endpoint (<code>POST {baseUrl}/v1/messages</code>). OpenAI-format endpoints cannot be plugged in directly here.',
  'providers.empty': 'No providers yet.',
  'providers.addProvider': 'Add provider',
  'providers.newLabel': 'New Provider',
  'providers.deleteSeat': 'Delete seat',
  'providers.modelLabel': "Model · blank = don't rewrite, forward as-is",
  'providers.baseUrlLabel': 'Base URL · must be an Anthropic Messages–format endpoint',
  'providers.baseUrlHint': 'router sends to <code>{baseUrl}/v1/messages</code>.',
  'providers.trailingV1Warning': 'A trailing <code>/v1</code> turns the real path into <code>/v1/v1/messages</code> — you probably want to drop it.',
  'providers.apiKeyCurrentHint': 'Current: {hint} (leave untouched to keep it)',
  'providers.apiKeyNotSet': 'not set',
  'providers.apiKeyStorageHint': 'Stored in plaintext in config.json, which is already in .gitignore.',
  'providers.authHeaderLabel': 'Auth header',
  'providers.maxTokensLabel': "max_tokens cap · blank = don't clamp",
  'providers.dropFieldsLabel': 'Dropped body fields · comma-separated · leave blank if unsure',
  'providers.dropsEffortWarning': 'Dropping <code>output_config</code> makes <code>/effort</code> <strong>fail silently</strong> — the request still returns 200, the model just gets dumber, with no error to warn you. The traffic log\'s Thinking column will show it as removed.',
  'providers.dropFieldsGuidance': 'By default nothing is dropped: a visible 400 beats a silent downgrade. If you do hit a 400, <strong>first turn off "Drop anthropic-beta header" above</strong> so the header and body arrive together as a pair; if that still doesn\'t fix it, set <code>CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1</code> on the Claude Code side; only then touch this field, and add one field at a time. Dropping <code>thinking</code> makes <code>kimi-k2.7-code</code> reject the request outright.',
  'providers.extraHeadersLabel': 'Extra headers · one Name: Value pair per line',
  'providers.dropBetaLabel': 'Drop <code>anthropic-beta</code> header',
  'providers.dropBetaHint': 'The official docs say the header and body fields are a <strong>pair</strong>: stripping only the header while leaving the body is exactly the combination that produces a hard <code>400</code> — the two halves have to be dropped together for a silent, clean switch-off. The default of <code>dropBeta: true</code> plus an empty <code>dropFields</code> happens to be "strip the header, keep the body" — measured against Kimi (1,668 requests) and DeepSeek with zero <code>400</code>s, so the default is left alone. If you do hit a 400, turn this off first so both halves arrive as a pair. The subscription line always keeps the header untouched — this setting never affects it.',
  'providers.testModelLabel': 'Test model · blank = use the one above',
  'providers.runTest': 'Run test',
  'providers.allPass': "All four passed. This provider can take Claude Code's subagent traffic.",
  'providers.somePassFail': "Something failed — if any of the first three fail, Claude Code won't work on this provider at all; if only the fourth fails, it'll work, but <code>/effort</code> gets eaten.",
  'providers.notTested': 'Not tested yet. The first three check basic inference, SSE streaming, and tool calling — if any fails, Claude Code won\'t work on this provider at all. The fourth checks whether <code>/effort</code> actually reaches the model; when it\'s broken there\'s no error, the model just gets dumber.',
  'providers.retryOverride': 'Retry overrides',
  'providers.retryOverrideCount': ' ({count} overridden)',
  'providers.retryOverrideEmpty': ' (blank = inherit global)',
  'providers.retry.attempts': 'Retry count',
  'providers.retry.attemptsHint': '0 = off',
  'providers.retry.baseDelay': 'Backoff start (ms)',
  'providers.retry.maxDelay': 'Backoff cap (ms)',
  'providers.retry.maxRetryAfter': 'retry-after cap (ms)',
  'providers.retry.maxRetryAfterHint': 'beyond this, hand it back to Claude Code',
  'providers.retry.throttle': 'Throttling (429)',
  'providers.testConnectivity': 'Basic inference',
  'providers.testStreaming': 'SSE streaming',
  'providers.testTools': 'Tool calling',
  'providers.testEffort': 'Thinking effort',
  'providers.testConfig': 'Config',

  // ── Routing 分頁（規則） ──────────────────────────────────────
  'rules.pickTarget': '— pick a target —',
  'rules.match.any': 'Any request',
  'rules.match.main': 'Main conversation only',
  'rules.match.subagent': 'All subagents (including Workflow / ultracode)',
  'rules.match.nested': 'Nested subagents only',
  'rules.enabledTitle': 'Enabled',
  'rules.wordModel': ' · model',
  'rules.wordAgent': ' · agent',
  'rules.wordSentAs': ' · sent as',
  'rules.modelGlobTitle': 'matches against the model name, supports *',
  'rules.agentGlobTitle': 'matches x-claude-code-agent-id, supports *. * = no filter',
  'rules.noRewritePlaceholder': "don't rewrite",
  'rules.deleteRule': 'Delete rule',
  'rules.hitExplain': "This request is caught by this rule. The rules below it aren't used this time.",
  'rules.order': 'Evaluation order',
  'rules.orderHint': 'Evaluated top to bottom, first match wins; the rules shadowed below are not used for this request',
  'rules.addRule': 'Add rule',
  'rules.empty': 'No rules — all traffic goes to the subscription.',
  'rules.floorHint': 'Everything that matches nothing lands here — sent as-is to <code>{baseUrl}</code>, billed to the subscription',
  'rules.floorLabel': 'Rack floor · cannot be deleted',
  'rules.tip.quota': '<strong>When a quota runs dry</strong>, just switch the rule\'s target from a provider to <code>SUB subscription</code> — no need to delete the rule, clear the API key, or restart. Settings are read fresh on every request, so the agent that\'s currently running reroutes on its very next request.',
  'rules.tip.disable': '<strong>Why not just "disable" it</strong>: disabling drops the traffic to the <strong>next</strong> rule, not back to the subscription. Only pointing explicitly at passthrough actually parks it there, and the rule stays in its place in the order.',
  'rules.tip.ultracode': '<strong>Covering ultracode requires "All subagents"</strong>. Workflow\'s <code>agent()</code> counts as a first-level subagent with no parent header — picking "Nested only" catches none of it.',
  'rules.tip.agentMatch': '<strong>Agent matching</strong> is for routing by an agent team\'s role: a teammate\'s id is derived from its name and stays stable across reconnects. A regular subagent gets a fresh one on every spawn, so it can\'t be filtered on. The main conversation never carries this header, so it never matches a pattern other than <code>*</code>.',
  'rules.previewTitle': 'Routing preview',
  'rules.previewHint': 'No need to actually spawn one',
  'rules.requestSource': 'Request source',
  'rules.kind.main': 'Main conversation',
  'rules.kind.subagent': 'Subagent',
  'rules.kind.nested': 'Nested subagent',
  'rules.requestedModelLabel': 'Requested model',
  'rules.agentIdLabel': 'agent id · optional',
  'rules.result.classifiedAs': 'Classified as',
  'rules.result.target': 'Target',
  'rules.result.matched': 'Matched',
  'rules.result.noMatch': 'No rule matched · falls to the rack floor',

  // ── Traffic 分頁 ──────────────────────────────────────────────
  'logs.filterNested': 'Nested',
  'logs.lastFetchFailed': "Last one: router couldn't reach upstream",
  'logs.lastBlocked': 'Last one: upstream blocked it',
  'logs.clientGot502': 'the client got a 502 the router synthesized',
  'logs.notThere': 'Not there at all',
  'logs.neverArrived': 'Request never reached the router',
  'logs.rackTitle': 'Traffic rack',
  'logs.pollHint': 'Refreshes every 3s · records metadata only, never prompt content',
  'logs.clear': 'Clear',

  // ── Advanced 分頁 ─────────────────────────────────────────────
  'advanced.intro': "The router's own settings. Except for <strong>the traffic log's filename and cap</strong> (the file sink is only created once at startup, so changing this needs a restart), everything else is read fresh on every request and takes effect as soon as you save.",
  'advanced.retryGlobalTitle': 'Retry · global defaults',
  'advanced.retryGlobalHint': "Each provider and passthrough can override a few of these fields on its own card; anything not overridden falls back to the values here. Retries only happen in the window before a single byte has been written to the client — once a stream starts, it can't be redone.",
  'advanced.retryAttemptsHint': '0 = off, every error is handed back as-is',
  'advanced.maxRetryAfterHint': "if upstream asks for a longer wait, don't absorb it — hand it back so Claude Code shows the countdown",
  'advanced.retryRateLimitLabel': 'Also retry on throttling (429)',
  'advanced.retryRateLimitHint': "Third-party 429s usually clear up after a short wait, so this is on by default. The subscription line is the opposite — passthrough overrides it to not retry by default: its 429 is the 5-hour quota window, and a few seconds of backoff won't recover from that.",
  'advanced.passthroughTitle': 'Default target · passthrough',
  'advanced.passthroughHint': "Where requests go when no rule matches, and when a rule explicitly points at passthrough. Credentials and headers are forwarded as-is; nothing in the body changes except a rule-specified model rewrite — this line is your subscription. Don't change it unless you've put another Anthropic-compatible front end in place.",
  'advanced.trafficLogTitle': 'Traffic log file',
  'advanced.trafficLogFileLabel': "Filename · relative to the config.json directory · blank = don't write to disk",
  'advanced.trafficLogFileHint': 'Metadata only, never prompts — but it does include project directories and session ids, so it is already in .gitignore by default',
  'advanced.maxBytesLabel': 'Rotation cap (bytes)',
  'advanced.maxBytesHint': 'Past this it rotates to <code>.1</code>, keeping only one old copy — at most double the disk usage',
  'advanced.requestLimitTitle': 'Request limit',
  'advanced.maxRequestBytesLabel': 'Single request body cap (bytes)',
  'advanced.maxRequestBytesHint': "The router keeps the whole body in memory so it can retry. This is a safety cap, not a throttle — a 1M-context request measured in the tens of MB.",

  // ── Connect 分頁 ──────────────────────────────────────────────
  'setup.step1Title': '1 · Configure Claude Code',
  'setup.step1Body': 'Add this to the <code>env</code> block of <code>~/.claude/settings.json</code> (Windows: <code>%USERPROFILE%\\.claude\\settings.json</code>):',
  'setup.warnCreds': '<strong>Never</strong> also set <code>ANTHROPIC_AUTH_TOKEN</code>, <code>ANTHROPIC_API_KEY</code>, or <code>apiKeyHelper</code>. The official docs are explicit: setting only the base URL without credentials keeps your claude.ai subscription login active; setting credentials replaces the subscription entirely, and all traffic switches to token billing.',
  'setup.attributionHint': '<code>CLAUDE_CODE_ATTRIBUTION_HEADER=0</code> is optional. Claude Code prepends an attribution block to the system prompt; only <code>api.anthropic.com</code> strips it automatically, so a third-party provider will receive it as part of the prompt. Turning it off is cleaner.',
  'setup.step2Title': '2 · Verify the subscription is still active',
  'setup.step2Body': 'Restart Claude Code, run <code>/status</code>, and check the <strong>Status</strong> tab:',
  'setup.step2Item1': 'The <code>Anthropic base URL</code> line appears and points at <code>http://127.0.0.1:{port}</code>',
  'setup.step2Item2': 'There is a <code>Login method</code> pointing at your claude.ai account — <strong style="color:var(--alarm)">not</strong> <code>Auth token</code> or <code>API key</code>',
  'setup.step2Note': 'Seeing <code>Auth token</code> means the subscription got replaced — check for leftover environment variables.',
  'setup.step3Title': '3 · Confirm the split is actually working',
  'setup.step3Body': 'Have Claude Code spawn a subagent to do anything, then go back to the <strong>Rack</strong> tab. The routing band should split into two segments: the main conversation\'s requests go through <code>SUB subscription</code>, and the subagent\'s go through <code>PRV</code> your configured provider.',
  'setup.limitsTitle': 'Known limitations',
  'setup.limit1': "Anthropic's official docs explicitly say pointing Claude Code at non-Claude models isn't supported — if it breaks, you fix it.",
  'setup.limit2': 'As of Claude Code v2.1.196, <strong>Remote Control is disabled</strong> whenever <code>ANTHROPIC_BASE_URL</code> points at a non-Anthropic host.',
  'setup.limit3': "<code>/fast</code>'s availability check and WebFetch's domain check connect directly to <code>api.anthropic.com</code>, bypassing this router.",
  'setup.limit4': "Every Claude Code upgrade can add new body fields; a third-party provider that doesn't recognize one will return a 400 — add the field name to that provider's dropped body fields based on the error message.",
  'setup.limit5': "If a provider doesn't support <code>/v1/messages/count_tokens</code> it returns 404, and Claude Code automatically falls back to estimating with the inference endpoint — this doesn't affect operation.",
}
