# Design

視覺世界：**航管進程條（flight progress strip bay）**。這份文檔從已建好的畫布回寫，不是先寫規則再去實作。
畫布：`design/*.dc.html`，已發佈的可點原型見 `design/model-router-console.html`。

## 論點

每一筆請求是一張**進條**，在**訂閱**與**第三方**兩個席位之間被交接。

這不是裝飾性的比喻，是同構：航管的進條機架要回答的問題（這架在誰手上、路線排定了沒、哪一張被扣住）跟 model-router 要回答的完全一樣（這筆由誰付帳、命中哪條規則、被誰擋下）。而進條機架處理「異常」的方式 —— 把那張條推出機架、用筆把原因寫在條上 —— 正好是這個產品最需要的東西：**舊版把失敗原因、重送次數、限流 header、命中規則全塞在 `title` tooltip 裡，README 得用整節教人該把滑鼠移到哪一格。**

明確拒絕的排法：深色管理後台的「側邊欄＋卡片網格＋頂部四顆統計方塊＋漸層折線圖」，以及它的反面「全黑底＋磷光綠＋等寬字當戲服」。

## 色彩

**策略：Restrained。** 中性底 ＋ 兩個等權席位色 ＋ 一個告警色。顏色在頁面尺度上成塊出現（分流帶、席位卡的實心邊條、進條的席位標籤塊），不是撒在中性底上的點綴。

**深色不是預設值，是從使用場景推出來的**：單人、夜間、第二螢幕、旁邊開著 terminal。亮底面板在那個環境裡會刺眼。

| 角色 | 值 | 用途 |
| --- | --- | --- |
| 機架底 | `#1b1f26` | 頁面底色。石墨藍，不是黑 |
| 機架凹槽 | `#151920` | 進條槽的內凹底 |
| 導軌 | `#2b323c` | 全部 hairline 分隔線與欄位骨架 |
| 導軌（亮） | `#3d4653` | 刻度的主刻線、scrollbar hover |
| 卡條紙 | `#e9e5db` | 進條的紙色。暖白，不是純白 |
| 卡條墨 | `#1a1e24` | 進條上的主要文字 |
| 卡條次墨 | `#635c50` | 進條上的次要文字。**從紙的色相調出來的暖灰，不是中性灰**。對卡條紙 4.77:1 |
| 機架文字 | `#dfe3e8` | 深底上的主要文字 |
| 機架次文字 | `#8b939f` | 深底上的次要文字與所有小標牌。對 `#1b1f26` 約 5.2:1 |

**席位編碼（唯二的分類色）**

| | 值 | oklch | 對 `#1b1f26` |
| --- | --- | --- | --- |
| SUB 訂閱 | `#43a08e` | 0.645 0.092 **178.4** | 5.24:1 |
| PRV 第三方 | `#ad8649` | 0.645 0.092 **77.5** | 4.95:1 |

兩者**明度與彩度完全相同、只差色相** —— 因為訂閱與第三方沒有高下之分，哪一邊在畫面上更搶眼都是說謊。這不是形容詞，是量出來的：兩個值的 L 與 C 逐位相同。（第一版用的 `#bd9047` 實測是 oklch(0.682 0.106 77.5)，比青色亮 5.7%、濃 15% —— 主張不成立，所以改了顏色而不是改文檔。）

**告警 `#e0563c`（oklch 0.631 **0.178** 32.7）是唯一被允許打破這個色階的顏色**：明度落在兩個席位色附近，彩度是它們的 1.93 倍。整個介面只有它可以這樣，所以它出現就一定是被擋下了。當小級數文字用時改用 `--alarm-ink #e8705a`（4.9:1），色塊與粗體標題才用原色。

規則：**飽和色只做編碼，永不承載正文**。沒有漸層、沒有顆粒、沒有裝飾性陰影。

## 字體

| 角色 | 畫布用（Google Fonts） | 落地用（Windows 自帶，零位元組） |
| --- | --- | --- |
| 標牌 `.sign` `.lbl` | Barlow Semi Condensed 500/600 | **`"Bahnschrift SemiCondensed"`** |
| 資料 `.num` | JetBrains Mono 400/500/700 | **Cascadia Mono** |
| 中文 `.han` `.disp` | Noto Sans TC | **Microsoft JhengHei UI** |

Barlow 與 Bahnschrift 都是 DIN／運輸標誌系的壓縮無襯線 —— 落地時換過去不是退讓，是同一個字族。三者已在目標機器上驗證存在，所以**正式 GUI 可以一個外部請求都不發**就有真正的儀表字體，維持單檔零依賴的約束。

**必須寫具名實例 `"Bahnschrift SemiCondensed"`**：Bahnschrift 是可變字體，只寫 `Bahnschrift` 會解析到預設寬度軸，壓縮性格在出貨版全部消失（已驗：這台機器上兩個實例都獨立註冊）。

**兩條硬規則，都是踩過才寫下來的：**

1. **`.num` 只給 token 與量測值，永遠不套在中文散文上。** `--data` 堆疊（Cascadia Mono / Consolas）**沒有任何漢字覆蓋** —— 套上去漢字會掉到宣告堆疊之外的系統字，跟同一行的拉丁字分屬兩個無關字族。中文說明走 `.hint`／`.han`。
2. **漢字不套 `.sign`。** 沒有 DIN 系的漢字壓縮體，而 `text-transform: uppercase` 對漢字無效、`letter-spacing` 只是把字距拉鬆。中文的顯示重量交給 `.disp`（字重 700、級數、零字距），不假裝它是標牌體。

分工因此是：**進條上的資料是拉丁字母與數字，走標牌體與等寬體；中文只承載說明與標題**，走人文體。等寬字在這裡是量測用途，不是「技術感」的戲服。所有數字 `font-variant-numeric: tabular-nums`。

## 幾何與材質

- **圓角一律 0。** 進條是方裁的印刷卡條。這是與舊版（11px 圓角卡片）最直接的分野。
- **席位色是實心標籤塊，不是彩色左邊框。** 46px 寬的色塊裡印著 `SUB`／`PRV` 與旗標縮寫 —— 它承載內容，而且避開了「卡片左邊一條粗彩色邊」這個 AI 生成 UI 最好認的特徵。
- **告警是整圈的框**（`box-shadow: 0 0 0 2px`），不是單邊重點色，而且框住整張條連同攤開的批註。
- **欄位骨架是畫出來的**：進條的欄位有實際的分隔線與表頭。值不在的時候，位置本身仍然讀得出來。
- 陰影只在真的需要層次時使用，且必須有位移與模糊。零位移的彩色光暈是裝飾，不用。

## 狀態：三個冗餘通道，顏色永遠不是唯一訊號

| 狀態 | 機架位置 | 印字旗標 | 旗標樣式 |
| --- | --- | --- | --- |
| 順利放行 | 齊平 | `CLR` | 深墨直接印在夾條上 |
| 成功但有事發生 | 推出 14px | `CHK` | **反白小塊**（深底、席位色字） |
| 被擋下 | 推出 30px | `HOLD` | 反白小塊 ＋ **整圈告警框** |

**第三個通道走明度與形狀，不再多發一個色相。** 夾條顏色永遠只編碼席位（青＝訂閱、琥珀＝第三方）；再發一個琥珀色去表示「有事發生」，就會讓同一個顏色在同一張畫面上有兩個意思。

旗標圖例**印在機架上**（總覽與流量兩處都有），而且圖例用的就是機架上真正的夾條樣式，不另發一套色票。旗標最小 10px、實色，不用透明度 —— 它是「顏色不是唯一訊號」的承載點，自己不能掉到對比地板以下。

**批註只染紅真的在講失敗的那幾行**（上游說法、重送、限流、靜默降級）。request-id、時間戳、請求形狀這些中性事實維持墨色 —— 整塊染紅是誇大。

## 版面

- flex／grid ＋ `gap`，不用 margin 撐開兄弟元素 —— 這樣拖拉重排、刪除、複製之後間距還在。
- 進條機架右側保留一欄 **批註欄**：航管的進條右側就是控制員寫字的地方。異常的一行摘要常駐在那裡（`上游擋的・3586s 後重置`、`/effort 被 dropFields 吃掉`、`cwd 表還沒建立`），不需要 hover。批註走漢字體，與同一列的等寬資料在字族上分開 —— 那是「這是寫上去的」與「這是量到的」的區別。
- 席位卡的**負載柱高度必須承載資料**：近 4 分鐘切成 8 個 30 秒桶，柱高就是那 30 秒的請求數，兩張席位卡共用同一個上限所以可以互相比較。一組高度固定、只靠填色格數表意的柱子是裝飾，不是資料 —— 而旁邊已經印著那個數字了。
- 機架標頭與進條槽的左右內距**必須逐像素對齊**（機架有 33px 右留白給推出的進條，標頭也要有）。骨架是畫出來的，差 3px 就等於沒畫。
- 說明貼著它解釋的控制項出現，不當開場白。舊版兩塊 500+ 字的散文擋在規則控制項前面，已拆成短的邊註。

## 動態

**靜止是預設。** 閒置時整面板死寂，沒有任何呼吸燈、脈動或裝飾性動畫。

三件事會動，其餘時間畫面是死的：
- **新進條印進機架** —— 唯一被著作的時刻，`.26s` 的下滑淡入，而且**只播給真的沒見過的 id**（記在一個 `SEEN` 集合裡）。每 3 秒輪詢一次全量重繪，若不比對 id 就會整面重播，那比不做動畫更糟。
- 進條推出／收回機架、hover 位移 —— `transform: translateX()`，`.16–.18s cubic-bezier(.16,1,.3,1)`。**不用 margin-left，那會每次都逼版面重排。**
- 分流帶兩段的寬度變化 —— `.45s`，同一條曲線。這裡刻意用 `width` 而不是 `transform: scaleX`，因為色段裡有排字，scaleX 會把字壓扁、也會謊報比例。代價是段內不能有左右 padding（`border-box` 會讓 0% 的段還留下 28px 的假殘影），所以內距改掛在段內的字上，而且 **0% 的段完全不畫**。

全部包在 `@media (prefers-reduced-motion: reduce)` 的關閉分支裡。

## 瀏覽器預設面

這些是「有沒有真的做過設計」最便宜的訊號，全部從色盤指定：`::selection`、scrollbar（track／thumb／hover）、`:focus-visible` 外框、連結色與 `text-underline-offset`、表格數字的 tabular-nums。

## 資訊架構

六格機架 register：**機架 · Providers · 路由 · 流量 · 進階 · 接入**，六個畫面共用同一組標籤與同一個 active 樣式。

「機架」是新增的總覽頁 —— 舊版沒有任何回答「現在到底正不正常」的畫面，這是這次重設計補上的最大缺口。它的第一屏是一條佔滿寬度的分流帶，一眼回答「有沒有在分流、比例多少」，而不是四顆統計方塊。（第一版在帶子下面還印了一把 0–100 刻度尺 —— 但帶內已經寫著「41 筆 · 28%」，刻度是把同一個百分比編碼第三次，已刪。）

## 文案

沿用產品既有語氣：實測導向、講原因不講口號、主動說出取捨與已知空窗。畫面上每一項事實都可以在 `model-router/README.md` 找到出處，包含那些不好看的：cwd 表還沒建立的空窗、200 但串流裡夾著 error 事件、訂閱線的 429 不帶 `retry-after`。**誠實優先於乾淨。**

術語不翻：`passthrough`、`subagent`、`nested`、`max_tokens`、`dropFields`、`output_config`、header 名。

## 由來

方向由 impeccable 的 `concept-seed` 從七個候選視覺世界中指派（seed key `d248776b`，assigned index 4，scope `direction`、mode `operate`；決策紀錄在 `.impeccable/decision.json`），使用者在決策頁鎖定 `assigned`、未重擲。五個被否決的挑戰者各捐出一項紀律，已寫進上面的規則：

- Crouwel 網格字樣本 → 骨架是畫出來的，而且本身是可切換的狀態
- 動物園導覽圖 → 平塗無顆粒，飽和色只做編碼、不承載正文
- 張拉整體柱 → 負載靠整個系統的形變表達，不靠徽章
- 劇場天幕破曉 → 狀態是具名的離散相位，同時帶標籤與圖樣
- Cracktro 捲動佇列 → 前景在被閱讀時完全靜止

一個 competitive 挑戰者（七段顯示家族）保留為完整替代方案：它在限流窗倒數那一個數字上贏得乾脆，但撐不起需要中文說明與表單的整個介面。

## 類別名不得與狀態名撞車

`.clr`（順利放行）與規則的排程句曾經共用同一個類別名，結果 `.slot.clr` 吃到了排程句的 `display:grid; 44px 1fr`，把接通測試的四張進條壓成一個字寬。排程句已改名 `.clearance`。

狀態名（`clr` / `chk` / `hold` / `live`）在這份設計裡是跨畫面共用的詞彙，任何新元件都不得再佔用這四個名字。
